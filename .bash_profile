#
# ~/.bash_profile
#
#for user Nick@archPC

[[ -f ~/.bashrc ]] && . ~/.bashrc

#set defaults for user
#export PATH=$PATH:$HOME/.scripts 
#Example for running commands in $PATH, TODO:
export EDITOR='nvim'
export TERMINAL='st'
export BROWSER='firefox'

#ly #On login, prompt login screen with DE
